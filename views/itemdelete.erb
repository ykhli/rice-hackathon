<% if @items %>  
  <h1>Are you sure you want to delete the following item: <em>"<%= @items.name %>"</em>?</h1>

  <p> Your item will be cast into Mount Doom<em>"<%=@items.name %>"</em>!</p>

  <form action="/item/<%= @items.id %>" method="post">  
    <input type="hidden" name="_method" value="delete">  
    <input type="submit" class="btn btn-danger btn-lg" value="Yes, Delete it!">  
    <a type="button" class="btn btn-info btn-lg" href="/item/<%= @items.id %>">Cancel!</a>  
  </form>  
  
<% else %>  
  <p>Item is on an adventure and cannot be located</p>  
<% end %>  
